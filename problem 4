#include <iostream>
#include <fstream>
using namespace std;

int main() {
    int n;
    cin >> n;
    ofstream out("count.txt");
    for (int i = 1; i <= n; i++) {
        if (i > 1) out << ' ';
        out << i;
    }
}
