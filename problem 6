#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    ifstream in("source.txt");
    ofstream out("copy.txt");
    string s;
    while (getline(in, s)) out << s << '\n';
}
