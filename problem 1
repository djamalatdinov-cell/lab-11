#include <iostream>
#include <fstream>
using namespace std;

int main() {
ifstream file("input.txt"); 
if (!file.is_open()) {      
cerr << "Cannot open input.txt\n";
return 1;
}
long long x, sum = 0;
while (file >> x) { 
sum += x;
}
cout << sum;
return 0;
}
