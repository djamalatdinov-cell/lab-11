#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ifstream in("numbers.txt");
    long long x, mx;
    if (in >> mx) {
        while (in >> x) mx = max(mx, x);
        cout << mx;
    }
}
