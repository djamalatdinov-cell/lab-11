#include <iostream>
#include <fstream>
using namespace std;

int main() {
    string name;
    cin >> name;
    ifstream in(name);
    char c;
    int cnt = 0;
    while (in.get(c)) cnt++;
    cout << cnt;
}
